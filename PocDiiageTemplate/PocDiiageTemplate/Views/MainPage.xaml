<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PocDiiageTemplate.Views.MainPage"
             Title="{Binding Title}">

    <Grid HorizontalOptions="Center"
          VerticalOptions="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Grid.Row="0" Grid.Column="0">
            <Label Text="Welcome to Xamarin Forms and Prism!" />
        </StackLayout>

        <Button Text="Navigate" Command="{Binding NavigateCommand}" Grid.Row="1" Grid.Column="0" />
        
    </Grid>

</ContentPage>